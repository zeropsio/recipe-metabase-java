use: [ ]
# which commands to run to build
run:
  - curl https://downloads.metabase.com/v0.41.2/metabase.jar --output metabase.jar
  - chmod a+x start.sh
# which files / folders to deploy after build
deploy: [ start.sh, metabase.jar ]